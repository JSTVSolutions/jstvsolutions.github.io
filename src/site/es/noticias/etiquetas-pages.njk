---js
// <script>
const eleventyExcludeFromCollections = true;

const layout = "page.njk";

const pagination = {
  data: "collections",
  size: 1,
  alias: "tag",
  filter: ["all", "posts"],
  // addAllPagesToCollections: true,
};

const eleventyComputed = {
  title: "{{ tag }}",
  subtitle: "Todas nuestras novedades y actualizaciones sobre {{ tag }}.",
  permalink: function(data) {
    return `/es/noticias/etiquetas/${this.slugify(data.tag)}/`;
  }
};
---

{%- set postlist = collections[ tag ] | matchPageLang %}
{%- include "postlist.njk" %}
